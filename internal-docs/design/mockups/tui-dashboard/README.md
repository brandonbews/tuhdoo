# TUI dashboard mockups (2026-07-31)

Design pass for the dashboard list's visual hierarchy. `current.*` is the
pre-redesign render for contrast; `mock-a`/`mock-b`/`mock-c` are the candidates.
**mock-a is the approved direction** — implemented by task t-01KYVJ2607S5S390CVYSF3PVG4,
whose description is the authoritative spec. `cat mock-a.ansi` in a terminal for
the colored render; the `.txt` twins are the colorless geometry; `mockups.py`
regenerates both.
