# Generated data-branch README: fix the wrong docs/ sentence, point at tuhdoo.com, tighten the prose

`tuh-01KZF1DNJ3T77A01NJXHW4QGAW`

- **Status:** open — in progress, claimed by `brandon/claude-code-4`
- **Priority:** 0
- **Labels:** `go` `ledger` `views`
- **Created:** 2026-08-07 21:17 UTC by `brandon/claude-code-1`

## Description

Context: captured at the 2026-08-07 strategy grill; grilled with Brandon 2026-08-11 — decisions below are his, do not re-litigate. `internal/views/views.go` `readme()` bakes "The design lives in `docs/` on the repository's main branch" into the generated data-branch README of every adopting repo. For an adopter, `docs/` is their own app's docs directory — the sentence was always a this-repo assumption, and after the docs swap (root docs/ = published content, working docs → internal-docs/) it is wrong here too.

Decisions (2026-08-11 grill):
- **Replace the sentence with a product pointer**: "Docs: https://tuhdoo.com" (site root, not a deep path — roots outlive routes). Rationale: the README's real audience is someone browsing an unfamiliar `tuhdoo` branch wondering what it is — a discovery moment worth serving, not dropping. Views regenerate on upgrade, so the link is changeable later; T2 untouched (a URL in generated markdown, no host API).
- **Tighten the README's remaining fixed prose in the same PR** to the utilitarian writing bar (the 2026-08-11 copy captures name the failure modes: em-dash cadence, fragments, fluff). These strings are generated code, so the copy-tightening task (scoped to docs/ + site) cannot reach them; this task owns them. Keep it short and concrete — the structure (intro, At a glance table, view links, format footer) stays.
- **Bump `FormatVersion` 7→8** (views.go:41) — T6: rendering change, cosmetic-only, highest-wins, so peers converge on one rendering instead of ping-ponging.

Acceptance:
- `readme()` output: no reference to the host repo's `docs/`; the tuhdoo.com pointer present; prose tightened; structure unchanged.
- `FormatVersion` = 8; view tests asserting README bytes updated.
- Brandon's PR review is the wording bar. `make test lint` green; one PR.

Constraints: rendering only — no event or state changes (T3), no verb changes (T5). Stored event bytes untouched; only generated view files change, via normal regeneration.

Pointers: `internal/views/views.go` — `readme()`, `FormatVersion` (line 41), the highest-wins guard comment (~line 66). The views package tests assert generated bytes — expect golden updates.

## History

### 2026-08-11 23:28 UTC — edit by `brandon/claude-code-4`

retitled · description edited · status inbox→open · labels +views
